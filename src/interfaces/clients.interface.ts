// custom data type for client record
export interface Client {
	name: String;
	email: String;
	password: String;
}

// custom data type for client account record
export interface ClientAccount {
	email: String;
	password: String;
}
